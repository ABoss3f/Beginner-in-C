/Given two grades, calculate and show the weighted average of these grades considering weight 2 for the first and weight 3 for the second./

#include<stdio.h>
#include<locale.h>

int main(){
	
	setlocale(LC_ALL,"portuguese");
	float num1;
	float num2;
	
	printf("Digite a primeira nota: \n");
	scanf("%f", &num1);
	
	printf("Digite o segunda nota: \n");
	scanf("%f", &num2);
	
	float result = (num1 * 2) + (num2 * 3);
	float media = result / 5;
	printf("O resultado  será = %f", media);
	
}
